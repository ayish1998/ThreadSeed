name: StoryWeave
version: 1.0.0
description: Collaborative storytelling game that brings Reddit communities together
author: [your-username]

# Reddit API permissions
permissions:
  - posts.read
  - posts.create
  - comments.read
  - comments.create
  - users.read
  - subreddits.read

# Web view configuration
webview:
  height: tall
  width: full

# App configuration
app:
  accountContext: true
  subredditContext: true